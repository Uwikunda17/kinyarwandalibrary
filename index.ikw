const PORT = 4500;
const HOST = 'localhost';
let SERVER_URL = 'http://';
SERVER_URL = SERVER_URL + HOST;
SERVER_URL = SERVER_URL + ':';
SERVER_URL = SERVER_URL + PORT;

umukoro tangaza_server(url) {
  andika('IKW server iri gukora kuri: ' + url);
}

tangaza_server(SERVER_URL);

export PORT;
export SERVER_URL;
